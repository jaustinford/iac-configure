---
ansible_user: "{{ secrets.ansible.remote_username }}"
ansible_password: "{{ secrets.ansible.remote_password }}"
ansible_become_password: "{{ secrets.ansible.remote_password }}"
ansible_ssh_private_key_file: /root/ssh.key
