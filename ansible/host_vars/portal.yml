---
ansible_python_interpreter: /usr/bin/python3

# ansible_user: "{{ names.users.host.super.name }}"
# ansible_password: "{{ secrets.users.host.super.portal }}"
# ansible_become_password: "{{ secrets.users.host.super.portal }}"

ansible_user: "{{ names.users.host.iac.name }}"
ansible_password: "{{ secrets.users.host.iac.portal }}"
ansible_become_password: "{{ secrets.users.host.iac.portal }}"

ansible_ssh_private_key_file: /iac-configure.key
