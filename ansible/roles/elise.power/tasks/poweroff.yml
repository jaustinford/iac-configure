---
- name: POWEROFF - shutdown host
  community.general.shutdown:
  become: true
  when: poweroff.enabled

- name: "POWEROFF - waiting on host port {{ wol.wait_port }}"
  delegate_to: localhost
  ansible.builtin.wait_for:
    host: "{{ ansible_host }}"
    port: "{{ wol.wait_port }}"
    state: stopped
    delay: 15
    sleep: 10
  when: poweroff.enabled
