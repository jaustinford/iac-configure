---
- name: "DIRECTORIES - copy container directory {{ item.path }}"
  ansible.builtin.file:
    path: "{{ folder_container_config }}/{{ item.path }}"
    state: "{{ item.state }}"
    owner: root
    group: "{{ names.groups.devops.name }}"
    mode: "0750"
  become: true
