---
- name: "templates - copy container template {{ item.path }}"
  template:
    src: "{{ item.src }}"
    dest: "/elysianskies/configurations/{{ item.path }}"
    mode: '0755'
    owner: "{{ item.owner }}"
    group: "{{ item.group }}"
  become: true
  notify: "main - docker restart {{ item.path.split('/')[0] }}"
