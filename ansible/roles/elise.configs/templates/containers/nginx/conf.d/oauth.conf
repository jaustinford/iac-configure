server {
    server_name oauth.{{ lab.domain }} oauth.proxy-ext.{{ lab.domain }};
    listen      443 ssl;

    if ($general_allowed_country = no) {
        return 403;
    }

    location / {
        proxy_pass http://{{ oauth_vlan_40_address }}:{{ ports.oauth_web }};
    }
}
