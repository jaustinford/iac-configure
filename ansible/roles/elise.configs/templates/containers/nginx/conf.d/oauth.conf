server {
    server_name oauth.{{ lab.domain }};
    listen      443 ssl;

    if ($allowed_country = no) {
        return 403;
    }

    location / {
        proxy_pass http://{{ oauth_vlan_40_address }}:{{ ports.oauth_web }};
    }
}
