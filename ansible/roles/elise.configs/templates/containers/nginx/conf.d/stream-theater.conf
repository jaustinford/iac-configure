server {
    server_name stream-theater.{{ lab.domain }} stream-theater.proxy-ext.{{ lab.domain }};
    listen      443 ssl;

    if ($plex_allowed_country = no) {
        return 403;
    }

    location / {
        proxy_pass http://{{ plex_vlan_40_address }}:{{ ports.plex_web }};
    }
}

server {
    server_name stream-theater.proxy-int.{{ lab.domain }};
    listen      443 ssl;

    if ($http_x_portal) {
        return 404;
    }

    location / {
        proxy_pass http://{{ plex_vlan_40_address }}:{{ ports.plex_web }};
    }
}
