---
- name: MAIN - include docker_hosts tasks
  ansible.builtin.include_tasks: docker_hosts.yml
  when: inventory_hostname != "nas"

- name: MAIN - include files_normal tasks
  ansible.builtin.include_tasks: files_normal.yml
  when: inventory_hostname == "nas"
  with_items:
    - non_privileged
