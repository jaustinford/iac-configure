---
- name: MAIN - include docker tasks
  ansible.builtin.include_tasks: docker.yml
  when: inventory_hostname != "nas"

- name: MAIN - include files tasks
  ansible.builtin.include_tasks: files.yml
  with_items:
    - .bash_profile
    - .vimrc
    - .hushlogin
