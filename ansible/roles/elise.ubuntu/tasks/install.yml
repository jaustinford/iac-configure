---
- name: INSTALL - install additional packages
  ansible.builtin.apt:
    name: "{{ item.name }}"
    state: "{{ item.state }}"
    update_cache: true
  become: true
  with_items:
    - "{{ ubuntu.additional_packages }}"
