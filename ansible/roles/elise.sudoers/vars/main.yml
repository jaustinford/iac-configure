---
sudoers:
  restrict_groups:
    cmd_paths:
      - /bin
      - /usr/bin

    docker_options:
      - exec
      - run

    discover_cmds:
      - ls
      - vi
      - vim
      - cat
      - nano
      - less
      - more

    discover_dirs:
      - /etc/sudoers
      - /usr/local/etc/sudoers
      - /root/.smb/.
      - "/mnt/{{ names.raid_vol.name }}"
      - /mnt/dfs_lvm
      - /lab
