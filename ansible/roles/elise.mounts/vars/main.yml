---
mounts:
  glusterfs:
    - host: "{{ names.hosts.docker01 }}.{{ lab.domain }}"
      state: "{{ cycle_mode.volumes.dfs_lvm }}"
      src: /dev/mapper/ubuntu--vg-dfs_lvm
      path: /mnt/dfs_lvm

    - host: "{{ names.hosts.docker02 }}.{{ lab.domain }}"
      state: "{{ cycle_mode.volumes.dfs_lvm }}"
      src: /dev/mapper/ubuntu--vg-dfs_lvm
      path: /mnt/dfs_lvm
