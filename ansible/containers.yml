---
- name: CONTAINERS - apply elise.containers role
  hosts: localhost
  roles:
    - elise.containers
  vars_files:
    - group_vars/concats.yml
    - group_vars/lab.yml
    - group_vars/ports.yml
    - group_vars/secrets.yml

- name: CONTAINERS - apply docker prune
  hosts: lab_onprem_docker
  tasks:
    - name: CONTAINERS - prune unused docker resources
      community.docker.docker_prune:
        containers: true
        images: true
        images_filters:
          dangling: false
        networks: true
        volumes: true
        builder_cache: true
      when: lookup('env', 'ELISE_PROJECT_CRON_TRIGGER') == "true"
      become: true
