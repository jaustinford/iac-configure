---
stages:
  - test
  - lint
  - integrate
  - deploy

default:
  tags:
    - elysianskies_runner

variables:
  ELISE_CI_NEXUS_HOSTED: docker01-teine.home.elysianskies.com:32501

include:
  - template: Jobs/Secret-Detection.gitlab-ci.yml
  - local: ci/build.yml
  - local: ci/ansible.yml
